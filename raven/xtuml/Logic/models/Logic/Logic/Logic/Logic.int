domain Logic is
    public service nextPeggingPlay (
        cards : in sequence of Game::CardType,        pile_cards : in sequence of Game::CardType    );
    public service nextDiscard (
        cards : in sequence of Game::CardType,        my_crib : in boolean    );
end domain;
