public service HeartRateMonitor::HeartRateMonitor.initialize () is
monitor: instance of HeartRateMonitor;
begin
  monitor := find_one HeartRateMonitor();
  if (null = monitor) then
    monitor := create HeartRateMonitor( id => 1, Current_State => idle );
  end if;
end service;
