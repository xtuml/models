//! This function is accessible for the whole domain.
public service Function_Calls::Domain_FunctionA (InputA  : in  integer,
                                                 OutputA : out integer) is
   if InputA = 1 then
      OutputA = 1
   else
      OutputA = 2
   endif
