// BP 7.1.6 content: InstanceStateMachine syschar: 3 persistence-version: 7.1.6

within ASL_Mapping::ASL_Mapping::ASL_Mapping::ASL_Mapping::Nested_IF is

	@event_num(1);
	event Fault;

	@event_num(2);
	event Finished;

	@event_num(3);
	event StartA;

	@event_num(4);
	event StartB;

	state model is


	end state model;

end;
