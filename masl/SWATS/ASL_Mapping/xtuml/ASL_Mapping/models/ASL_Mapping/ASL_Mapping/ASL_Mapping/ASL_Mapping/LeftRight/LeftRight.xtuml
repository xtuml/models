// BP 7.1.6 content: ModelClass syschar: 3 persistence-version: 7.1.6

within ASL_Mapping::ASL_Mapping::ASL_Mapping::ASL_Mapping is

	@key_letters("objLR");
	@class_num(19);
	class LeftRight is

		@ref_mode("local");
		idL: referential (R1.Left.idL) integer;

		@ref_mode("local");
		idR: referential (R1.Right.idR) integer;

		@ref_mode("local");
		idAR: referential (R2.Another_Right.idAR) integer;

		preferred identifier is (idR);

		identifier is (idL);

	end class;

end;
