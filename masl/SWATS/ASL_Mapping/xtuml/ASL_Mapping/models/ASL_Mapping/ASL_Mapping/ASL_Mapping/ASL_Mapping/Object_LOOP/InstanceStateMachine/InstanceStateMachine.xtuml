// BP 7.1.6 content: InstanceStateMachine syschar: 3 persistence-version: 7.1.6

within ASL_Mapping::ASL_Mapping::ASL_Mapping::ASL_Mapping::Object_LOOP is

	@event_num(1);
	event Start;

	@event_num(2);
	event Finished;

	@event_num(3);
	event Fault;

	state model is


	end state model;

end;
