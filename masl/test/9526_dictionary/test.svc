public service dict::test() is
  sparseArray : dictionary integer of string;
  i : integer;
  s : string;
  b : boolean;
begin
  sparseArray[i] := s;
  s := sparseArray[i];

  b := sparseArray'contains(3); 
  // b := sparseArray'contains("hello"); // ERROR 

  i := sparseArray'length();
  erase sparseArray[i];
end service;
