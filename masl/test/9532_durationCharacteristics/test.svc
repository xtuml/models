public service duration::test() is
  d : duration;
begin
  d := duration'days(1.5);
  d := duration'hours(1.5);
  d := duration'minutes(1.5);
  d := duration'seconds(1.5);
  d := duration'milliseconds(1.5);
  d := duration'microseconds(1.5);
  d := duration'nanoseconds(1.5);
end service;