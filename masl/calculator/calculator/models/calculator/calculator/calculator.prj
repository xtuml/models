project calculator is
domain keypad is
  terminator keyIO is
    public service key (
        code : in integer    );
    public service clear (
    );
  end terminator;
end domain;
domain ALU is
  terminator disp is
    public service result (
        value : in real    );
    public service error (
        message : in string    );
  end terminator;
end domain;
end project;
