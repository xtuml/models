domain keypad is
    public service error (
        message : in string    );
    public service result (
        value : in real    );
  terminator keyIO is
    public service key (
        code : in integer    );
    public service clear (
    );
  end terminator;
end domain;
pragma number ( 2 ); 
