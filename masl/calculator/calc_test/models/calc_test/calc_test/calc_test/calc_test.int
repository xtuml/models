domain calc_test is
    public service result (
        value : in real    );
    public service error (
        message : in string    );
  terminator keyIO is
    public service key (
        code : in integer    );
    public service clear (
    );
  end terminator;
end domain;
pragma number ( 3 ); 
